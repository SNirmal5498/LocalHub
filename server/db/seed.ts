/**
 * LocalHub Database Initialization
 *
 * In production mode, LocalHub does NOT seed demo storefronts, fake accounts,
 * fake products, fake orders, or fake reviews.
 * Real data enters the platform via genuine merchant and customer operations.
 */
export async function seedInitialData(): Promise<void> {
  // No-op in production. Data is created organically by real users.
}
